// EXAMPLE: How the controller is used
// src/index.js does this:
// const { getAvailableProducts } = require("../controllers/productController");
// app.get("/products/available", getAvailableProducts);
//
// Student task: only write logic inside getAvailableProducts(req, res)
// Routing is already done.
