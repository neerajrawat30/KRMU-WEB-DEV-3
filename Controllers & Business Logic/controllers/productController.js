const products = require("../data/products");

const getAvailableProducts = (req, res) => {
    // TODO: Write business logic here
    //
    // Steps:
    // 1. Filter products to keep only items where inStock is true
    // 2. Return status 200
    // 3. Send filtered products as JSON
};

module.exports = {
    getAvailableProducts
};
