const employees = require("../data/employees");

const getActiveEmployees = (req, res) => {
  // TODO: Implement the controller to return only active employees

  
  // Steps:
  // 1. Filter the employees array to return only active employees
  //
  // 2. Return response with status 200 and filtered array as JSON

  // See controllers/example.js to understand how this controller is used
  // Expected to return 3 employees: Rahul, Priya, Rohit

};

module.exports = {
    getActiveEmployees,
};
